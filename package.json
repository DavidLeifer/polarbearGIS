{
  "name": "ol-ext-parcel-bundler",
  "version": "1.0.0",
  "description": "ol-ext + parcel bundler",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "parcel index.html -p 700 --out-dir ./.dist",
    "build": "parcel build index.html --public-url ./"
  },
  "keywords": [
    "ol",
    "ol-ext",
    "parcel"
  ],
  "author": "Viglino",
  "license": "MIT",
  "devDependencies": {
    "cssnano": "^4.1.10",
    "parcel-bundler": "^1.9.4"
  },
  "dependencies": {
    "http-proxy": "^1.18.1",
    "http-proxy-rules": "^1.1.3",
    "ol": "^5.3.3",
    "ol-ext": "^3.0.0"
  }
}
